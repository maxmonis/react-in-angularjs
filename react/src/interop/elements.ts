import { InternalLink } from '../components/internal-link'

// Add components here to expose them to the Angular app.
// Note that these must each include a unique `tag` property.
// e.g. tag: `my-component` -> <my-component></my-component>
export const elements = [InternalLink]
